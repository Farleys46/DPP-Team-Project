services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: air_flight_db
    ports:
      - "5432:5432"

  openaq-service:
      build: ./OpenAQ-fetcher
      env_file: .env
      depends_on:
        - db

  opensky-service:
      build: ./OpenSky-fetcher
      env_file: .env
      depends_on:
        - db

  dashboard:
       build: .
       commands:
       ports:
         - 
       env_file: .env
       depends_on:
         - db